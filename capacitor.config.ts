import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'promo.exemple.com',
  appName: 'musique-promo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
